SELECT f.*
FROM file_log f
INNER JOIN (
  SELECT template_id, MAX(file_date) AS max_file_date
  FROM file_log
  GROUP BY template_id
) m
ON f.template_id = m.template_id AND f.file_date = m.max_file_date
