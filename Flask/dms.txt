import time
import requests

# Replace this with the URL of your service
service_url = 'http://your-service-url.com'

# Number of requests to send
num_requests = 1000000

# Function to send requests and measure time
def test_service_performance():
    start_time = time.time()

    for i in range(num_requests):
        response = requests.get(service_url)
        # You can handle the response here if needed

    end_time = time.time()
    elapsed_time = end_time - start_time

    return elapsed_time

if __name__ == '__main__':
    total_time = test_service_performance()
    print(f"Processed {num_requests} requests in {total_time:.2f} seconds.")
    print(f"Requests per second: {num_requests / total_time:.2f}")

